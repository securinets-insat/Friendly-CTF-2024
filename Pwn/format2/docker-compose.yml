version: '3'
services:
  format2:
    build: .
    ports:
      - 5019:10001
    read_only: true
    cap_drop:
      - all
              
    restart: on-failure

    deploy:
        resources:
            limits:
              cpus: '0.2'
              memory: 1000M