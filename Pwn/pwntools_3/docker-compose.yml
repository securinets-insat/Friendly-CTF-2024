version: '3'
services:
  pwntools_3:
    build: .
    ports:
      - 5004:10001
    read_only: true
    cap_drop:
      - all
              
    restart: on-failure

    deploy:
        resources:
            limits:
              cpus: '0.2'
              memory: 1000M