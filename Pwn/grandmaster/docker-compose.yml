version: '3'
services:
  grandmaster:
    build: .
    ports:
      - 5022:10001
    read_only: true
    cap_drop:
      - all
              
    restart: on-failure

    deploy:
        resources:
            limits:
              cpus: '0.2'
              memory: 1000M